<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>fentwums.th-koeln.de</groupId>
    <artifactId>fentwums-fsm-parent</artifactId>
    <version>0.1.1</version>
    <packaging>pom</packaging>

    <modules>
        <module>fentwums-fsm</module>
        <module>fentwums-fsms</module>
    </modules>

    <properties>
        <!-- Definieren von Properties -->
        <java.version>21</java.version>
        <encoding>UTF-8</encoding>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>

        <!-- Dependencies Properties -->
        <dependency.version.junit>4.13.2</dependency.version.junit>
        <dependency.version.lombok>1.18.42</dependency.version.lombok>
        <dependency.version.picocli>4.7.7</dependency.version.picocli>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${dependency.version.lombok}</version>
            </dependency>
            <dependency>
                <groupId>info.picocli</groupId>
                <artifactId>picocli</artifactId>
                <version>${dependency.version.picocli}</version>
            </dependency>
            <dependency>
                <groupId>junit</groupId>
                <artifactId>junit</artifactId>
                <version>${dependency.version.junit}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version> <!-- Oder eine aktuelle Version -->
                <configuration>
                    <source>${java.version}</source> <!-- Oder 11, 17 etc., je nach Bedarf -->
                    <target>${java.version}</target> <!-- Oder 11, 17 etc., je nach Bedarf -->
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>